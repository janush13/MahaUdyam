import React, { useMemo, useState } from 'react';
import { useAuth } from '../../context/AuthContext';
import { LeadershipLayout } from '../../components/layout/LeadershipLayout';
import {
  LEADERSHIP_DISTRICTS,
  LEADERSHIP_PERIODS,
  LEADERSHIP_SECTORS,
  leadershipAnalyticsService,
} from '../../services/leadershipAnalyticsService';

const toneBar: Record<string, string> = { red: 'bg-red-600', amber: 'bg-amber-500', blue: 'bg-blue-600', green: 'bg-emerald-600' };

export const LeadershipDashboardPage: React.FC = () => {
  const { user } = useAuth();
  const [activeSection, setActiveSection] = useState('department');
  const [period, setPeriod] = useState(LEADERSHIP_PERIODS[0]);
  const [district, setDistrict] = useState(LEADERSHIP_DISTRICTS[0]);
  const [sector, setSector] = useState(LEADERSHIP_SECTORS[0]);
  const [notice, setNotice] = useState('');
  const [selectedDepartment, setSelectedDepartment] = useState<string | null>(null);
  const departments = useMemo(() => leadershipAnalyticsService.getDepartments(), []);
  const kpis = useMemo(() => leadershipAnalyticsService.getKpis(), []);
  const bottlenecks = useMemo(() => leadershipAnalyticsService.getBottlenecks(), []);

  const announce = (message: string) => {
    setNotice(message);
    window.setTimeout(() => setNotice(''), 2600);
  };
  const exportBrief = () => {
    const content = `MahaUdyam One Cabinet Brief (Prototype)\\nPeriod: ${period}\\nDistrict: ${district}\\nSector: ${sector}\\nRTS SLA Compliance: 94.8%`;
    const link = document.createElement('a');
    link.href = URL.createObjectURL(new Blob([content], { type: 'text/plain' }));
    link.download = 'mahaudyam-cabinet-brief.txt';
    link.click();
    URL.revokeObjectURL(link.href);
    announce('Cabinet Brief export generated from deterministic prototype analytics.');
  };
  const selectSection = (section: string) => {
    setActiveSection(section);
    const target = section === 'department' ? 'department-performance' : section === 'bottleneck' || section === 'sla' ? 'bottlenecks-panel' : section === 'scheme' ? 'scheme-analytics' : section === 'grievance' ? 'grievance-analytics' : 'dashboard-filters';
    document.getElementById(target)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };

  return (
    <LeadershipLayout activeSection={activeSection} onSectionChange={selectSection} onNotice={announce}>
      <main className="flex-1 p-4 sm:p-5 overflow-y-auto" data-purpose="leadership-workspace">
        {notice && <div role="status" className="fixed right-5 bottom-5 z-50 max-w-sm rounded-lg bg-[#0b2545] px-4 py-3 text-xs font-semibold text-white shadow-xl">{notice}</div>}
        <div id="dashboard-filters" className="bg-white p-3 rounded-lg border border-slate-300 shadow-sm mb-4 flex flex-wrap items-center justify-between gap-3" data-purpose="dashboard-filters">
          <div><h2 className="text-lg font-bold text-slate-800 tracking-tight">Department Performance &amp; Clearances Monitoring</h2><p className="text-xs text-slate-500">Real-time statutory clearance metrics across notified Government bodies</p><p className="text-[10px] text-amber-700 mt-1 font-semibold">Prototype analytics — not live government statistics</p></div>
          <div className="flex flex-wrap items-center gap-2">
            <label className="flex items-center bg-slate-50 border border-slate-300 rounded px-2.5 py-1 text-xs"><span className="text-slate-500 mr-1.5 font-medium">Period:</span><select aria-label="Period filter" value={period} onChange={(e) => setPeriod(e.target.value)} className="bg-transparent font-semibold text-slate-800 border-none p-0 text-xs focus:ring-0">{LEADERSHIP_PERIODS.map((item) => <option key={item}>{item}</option>)}</select></label>
            <label className="flex items-center bg-slate-50 border border-slate-300 rounded px-2.5 py-1 text-xs"><span className="text-slate-500 mr-1.5 font-medium">District:</span><select aria-label="District filter" value={district} onChange={(e) => setDistrict(e.target.value)} className="bg-transparent font-semibold text-slate-800 border-none p-0 text-xs focus:ring-0">{LEADERSHIP_DISTRICTS.map((item) => <option key={item}>{item}</option>)}</select></label>
            <label className="flex items-center bg-slate-50 border border-slate-300 rounded px-2.5 py-1 text-xs"><span className="text-slate-500 mr-1.5 font-medium">Sector:</span><select aria-label="Industry sector filter" value={sector} onChange={(e) => setSector(e.target.value)} className="bg-transparent font-semibold text-slate-800 border-none p-0 text-xs focus:ring-0">{LEADERSHIP_SECTORS.map((item) => <option key={item}>{item}</option>)}</select></label>
            <button type="button" onClick={exportBrief} className="bg-[#0b2545] hover:bg-[#133e68] text-white text-xs font-medium px-3 py-1.5 rounded shadow-sm focus:outline-none focus:ring-2 focus:ring-amber-400">⇩ Cabinet Brief (PDF)</button>
          </div>
        </div>
        <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-6 gap-3 mb-5" data-purpose="summary-kpis">{kpis.map((kpi) => <div key={kpi.label} className={`bg-white p-3 rounded-lg border border-slate-300 shadow-sm ${kpi.label === 'RTS SLA Compliance' ? 'bg-gradient-to-br from-white to-emerald-50/40' : ''}`}><div className="text-[11px] font-medium text-slate-500 uppercase tracking-wider">{kpi.label}</div><div className={`mt-1 flex items-baseline justify-between ${kpi.tone === 'blue' ? 'text-blue-700' : kpi.tone === 'green' ? 'text-emerald-700' : kpi.tone === 'red' ? 'text-red-600' : 'text-slate-900'}`}><span className="text-xl font-bold">{kpi.value}</span><span className="text-[10px] font-semibold bg-slate-50 px-1 py-0.5 rounded border border-slate-200">{kpi.badge}</span></div><div className="text-[10px] text-slate-400 mt-1">{kpi.detail}</div></div>)}</div>
        <div className="border-b border-slate-300 mb-4 flex gap-6 text-xs font-medium overflow-x-auto"><button type="button" onClick={() => selectSection('department')} className={`py-2 border-b-2 whitespace-nowrap ${activeSection === 'department' ? 'border-[#133e68] text-[#133e68] font-bold' : 'border-transparent text-slate-500 hover:text-slate-800'} focus:outline-none focus:ring-2 focus:ring-blue-600`}>1. Department-wise Performance (Panel 8) <span className="bg-blue-100 text-[#0b2545] text-[10px] px-1.5 py-0.5 rounded-full">6 Departments</span></button><button type="button" onClick={() => selectSection('bottleneck')} className={`py-2 border-b-2 whitespace-nowrap ${activeSection === 'bottleneck' ? 'border-[#133e68] text-[#133e68] font-bold' : 'border-transparent text-slate-500 hover:text-slate-800'} focus:outline-none focus:ring-2 focus:ring-blue-600`}>2. Process Bottlenecks &amp; Delays (Panel 9)</button><button type="button" onClick={() => selectSection('scheme')} className={`py-2 border-b-2 whitespace-nowrap ${activeSection === 'scheme' ? 'border-[#133e68] text-[#133e68] font-bold' : 'border-transparent text-slate-500 hover:text-slate-800'} focus:outline-none focus:ring-2 focus:ring-blue-600`}>3. Government Schemes Analytics (Panel 10)</button><button type="button" onClick={() => selectSection('grievance')} className={`py-2 border-b-2 whitespace-nowrap ${activeSection === 'grievance' ? 'border-[#133e68] text-[#133e68] font-bold' : 'border-transparent text-slate-500 hover:text-slate-800'} focus:outline-none focus:ring-2 focus:ring-blue-600`}>4. RTS Grievances &amp; Appeals (Panel 11)</button></div>
        <section id="department-performance" className="bg-white rounded-lg border border-slate-300 shadow-sm mb-5 p-4" data-purpose="department-performance"><div className="flex items-center justify-between mb-3 gap-3"><div><h3 className="text-sm font-bold text-slate-800">State Department Clearance Volumes &amp; Disposals</h3><p className="text-[11px] text-slate-500">Cross-department comparison: Total applications submitted, granted, and rejected</p></div><div className="flex items-center gap-3 text-xs"><span><i className="inline-block w-3 h-3 rounded-sm bg-blue-600 mr-1" />Submitted</span><span><i className="inline-block w-3 h-3 rounded-sm bg-emerald-600 mr-1" />Approved</span><span><i className="inline-block w-3 h-3 rounded-sm bg-red-600 mr-1" />Rejected</span></div></div><div className="bg-slate-50 p-4 rounded border border-slate-200 mb-4"><div className="h-44 flex items-end justify-between gap-2 sm:gap-5 px-2 border-b border-slate-300 pb-2">{departments.map((row) => <div key={row.name} className="flex-1 flex flex-col items-center min-w-0"><div className="w-full flex items-end justify-center gap-0.5 h-36" title={`${row.name}: ${row.applications} submitted, ${row.approved} approved, ${row.rejected} rejected`}><div className="w-1/4 max-w-5 bg-blue-600 rounded-t" style={{ height: `${Math.round(row.applications / 1400 * 100)}%` }} /><div className="w-1/4 max-w-5 bg-emerald-600 rounded-t" style={{ height: `${Math.round(row.approved / 1400 * 100)}%` }} /><div className="w-1/4 max-w-5 bg-red-600 rounded-t" style={{ height: `${Math.round(row.rejected / 1400 * 100)}%` }} /></div><span className="text-[10px] sm:text-[11px] font-semibold text-slate-700 mt-2 text-center truncate w-full">{row.shortName}</span></div>)}</div><div className="flex justify-between text-[10px] text-slate-500 pt-1"><span>Scale: 0 - 1,400 clearances</span><span>Source: Deterministic prototype dataset</span></div></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-xs border border-slate-200 divide-y divide-slate-200"><thead className="bg-slate-100 text-[11px] font-bold text-slate-700 uppercase tracking-wider"><tr><th className="py-2 px-3">Department Name</th><th className="text-right px-3">Applications</th><th className="text-right px-3">Approved</th><th className="text-right px-3">Rejected</th><th className="text-right px-3">Avg. Turnaround (Days)</th><th className="text-center px-3">SLA Compliance</th><th className="text-center px-3">Review Action</th></tr></thead><tbody className="divide-y divide-slate-200">{departments.map((row) => <tr key={row.name} className={`hover:bg-slate-50 ${selectedDepartment === row.name ? 'bg-blue-50' : ''}`}><td className="py-2 px-3 font-bold text-[#0b2545]"><span className={`inline-block w-2 h-2 rounded-full mr-1.5 ${row.tone === 'amber' ? 'bg-amber-600' : row.tone === 'green' ? 'bg-emerald-600' : 'bg-blue-600'}`} />{row.name}</td><td className="text-right px-3 font-mono font-semibold">{row.applications.toLocaleString('en-IN')}</td><td className="text-right px-3 font-mono text-emerald-700 font-semibold">{row.approved.toLocaleString('en-IN')}</td><td className="text-right px-3 font-mono text-red-600">{row.rejected}</td><td className={`text-right px-3 font-mono ${row.turnaroundDays > 20 ? 'text-amber-700 font-bold' : ''}`}>{row.turnaroundDays}</td><td className="text-center px-3"><span className={`inline-block px-2 py-0.5 rounded text-[11px] font-bold ${row.slaCompliance < 75 ? 'bg-amber-100 text-amber-800 border border-amber-300' : 'bg-emerald-100 text-emerald-800 border border-emerald-300'}`}>{row.slaCompliance}%</span></td><td className="text-center px-3"><button type="button" onClick={() => { setSelectedDepartment(row.name); announce(`${row.name} drilldown selected for ${period}, ${district}, ${sector}.`); }} className="text-blue-700 hover:text-blue-900 font-semibold hover:underline focus:outline-none focus:ring-2 focus:ring-blue-600">Drilldown</button></td></tr>)}</tbody></table></div></section>
        <div className="grid grid-cols-1 xl:grid-cols-12 gap-5 mb-5"><section id="bottlenecks-panel" className="xl:col-span-7 bg-white rounded-lg border border-slate-300 shadow-sm p-4" data-purpose="bottlenecks-panel"><div className="flex items-center justify-between pb-2 mb-3 border-b border-slate-200"><div><h3 className="text-sm font-bold text-slate-800">Process Bottlenecks &amp; Delays</h3><p className="text-[11px] text-slate-500">Identify stages with maximum procedural pendency</p></div><span className="text-[10px] bg-red-100 text-red-800 font-bold px-2 py-0.5 rounded border border-red-200">Scrutiny Bottleneck Flagged</span></div><div className="space-y-3">{bottlenecks.map((stage) => <div key={stage.label}><div className="flex justify-between text-xs font-semibold mb-1 text-slate-700 gap-2"><span className="flex items-center gap-1.5"><span className={`w-2 h-2 rounded-full ${stage.tone === 'red' ? 'bg-red-600' : stage.tone === 'amber' ? 'bg-amber-500' : stage.tone === 'green' ? 'bg-emerald-600' : 'bg-blue-600'}`} />{stage.label}</span><span className={`font-mono ${stage.overdue ? 'text-red-700' : stage.tone === 'amber' ? 'text-amber-700' : stage.tone === 'green' ? 'text-emerald-700' : 'text-slate-700'}`}>{stage.value}</span></div><div className="w-full bg-slate-200 rounded-full h-3"><div className={`${toneBar[stage.tone]} h-3 rounded-full`} style={{ width: `${stage.width}%` }} /></div></div>)}</div><div className="mt-4 p-3 bg-amber-50/70 border border-amber-200 rounded text-xs leading-relaxed"><div className="font-bold text-amber-900 mb-1">⚠ Statutory Bottleneck Insights</div><ul className="list-disc list-inside space-y-1 text-slate-700 text-[11px]"><li><strong>Maximum delays</strong> observed in application scrutiny stage at MPCB and Urban Development desks.</li><li>Inspection scheduling delays are prominent in Environment and Industries departments (avg. 4.2 days overdue).</li><li>Average overall turnaround reduced to <strong>18 days</strong> from 26 days after RTS auto-escalation integration.</li></ul></div></section><div className="xl:col-span-5 space-y-4"><section id="scheme-analytics" className="bg-white rounded-lg border border-slate-300 shadow-sm p-4" data-purpose="scheme-analytics"><div className="flex items-center justify-between pb-2 border-b border-slate-200"><h3 className="text-sm font-bold text-slate-800">Government Schemes &amp; Direct Benefit</h3><span className="text-[10px] text-blue-800 bg-blue-50 px-2 py-0.5 rounded font-mono font-bold border border-blue-200">Panel 10</span></div><div className="grid grid-cols-3 gap-2 my-3 text-center"><div className="bg-slate-50 p-2 rounded border border-slate-200"><div className="text-[10px] text-slate-500 uppercase">Beneficiaries</div><div className="text-base font-bold text-[#0b2545] mt-0.5">1,248</div><div className="text-[9px] text-slate-400">Applications</div></div><div className="bg-emerald-50 p-2 rounded border border-emerald-200"><div className="text-[10px] text-emerald-800 uppercase font-semibold">Approved</div><div className="text-base font-bold text-emerald-700 mt-0.5">892</div><div className="text-[9px] text-emerald-600">71.4% Sanctioned</div></div><div className="bg-amber-50 p-2 rounded border border-amber-200"><div className="text-[10px] text-amber-800 uppercase font-semibold">Disbursed</div><div className="text-base font-bold text-amber-700 mt-0.5">₹ 320 Cr</div><div className="text-[9px] text-amber-600">State Incentive</div></div></div><div className="p-2 bg-slate-50 rounded border border-slate-200"><div className="flex justify-between text-[11px] text-slate-600 font-medium mb-1"><span>Scheme Applications vs Disbursals</span><span className="text-emerald-700 font-bold">+18.5% Growth MoM</span></div><svg className="w-full h-8 text-blue-600" fill="none" viewBox="0 0 300 40" aria-label="Scheme applications versus disbursals trend"><path d="M0 35 Q 50 25, 100 28 T 200 15 T 300 5" fill="none" stroke="#2563eb" strokeWidth="2.5" /><path d="M0 38 Q 50 32, 100 30 T 200 22 T 300 12" fill="none" stroke="#16a34a" strokeDasharray="3 3" strokeWidth="2" /></svg><div className="flex justify-between text-[9px] text-slate-400"><span>Jan</span><span>Apr</span><span>Jul</span><span>Oct</span><span>Dec</span></div></div></section><section id="grievance-analytics" className="bg-white rounded-lg border border-slate-300 shadow-sm p-4" data-purpose="grievance-analytics"><div className="flex items-center justify-between pb-2 border-b border-slate-200"><div><h3 className="text-sm font-bold text-slate-800">Grievance Redressal (RTS Sec 19)</h3><span className="text-[10px] text-slate-500">First &amp; Second Appellate Authority review</span></div><span className="text-[10px] text-slate-700 bg-slate-100 px-2 py-0.5 rounded font-mono font-bold border border-slate-300">Panel 11</span></div><div className="flex items-center justify-between mt-3 gap-3"><div className="w-24 h-24 relative flex items-center justify-center shrink-0"><svg className="w-24 h-24 transform -rotate-90" viewBox="0 0 36 36" aria-label="78 percent grievances resolved"><path className="text-slate-200" d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="currentColor" strokeWidth="4.5" /><path className="text-emerald-600" d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" fill="none" stroke="currentColor" strokeDasharray="78, 100" strokeWidth="4.5" /></svg><div className="absolute text-center"><span className="text-base font-bold text-slate-800">230</span><div className="text-[9px] text-slate-500 uppercase leading-tight">Total</div></div></div><div className="flex-1 space-y-1.5 text-xs"><div className="flex justify-between"><span className="text-slate-600">● Resolved:</span><span className="font-bold text-emerald-700 font-mono">180 (78%)</span></div><div className="flex justify-between"><span className="text-slate-600">● Pending Scrutiny:</span><span className="font-bold text-amber-700 font-mono">35 (15%)</span></div><div className="flex justify-between"><span className="text-slate-600">● Reopened / Escalated:</span><span className="font-bold text-red-600 font-mono">15 (7%)</span></div><div className="pt-1 border-t border-slate-200 flex justify-between text-[11px]"><span className="text-slate-500">Avg. Resolution:</span><span className="font-bold text-[#0b2545]">12 Days (-20%)</span></div></div></div><div className="mt-3 pt-2 border-t border-slate-100 flex justify-end"><button type="button" onClick={() => announce('Grievance escalation queue is prototype-only.')} className="text-xs font-bold text-blue-700 hover:text-blue-900 focus:outline-none focus:ring-2 focus:ring-blue-600">Access Grievance Escalation Queue →</button></div></section></div></div>
      </main>
    </LeadershipLayout>
  );
};
